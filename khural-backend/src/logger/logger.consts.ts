enum EHotelActions {
	Create,
	Update,
	Delete,
}

enum EBookingActions {
	Create,
	Update,
	Delete,
	BookingStarted,
	BookingCompleted,
}

enum EOtpActions {
	Auth,
	SignUp,
}

export { EOtpActions };
