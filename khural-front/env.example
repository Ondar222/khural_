## Frontend environment variables (Vite)
##
## By default in dev (localhost) the app uses "/api" and Vite proxies it to http://localhost:4000
## (see vite.config.js). So you usually DON'T need VITE_API_BASE_URL locally.

# Optional: override API base URL (example for direct backend access)
# VITE_API_BASE_URL=http://localhost:4000

# Optional: storage keys (defaults: access_token / refresh_token; legacy auth_token is also supported)
# VITE_API_TOKEN_STORAGE_KEY=access_token
# VITE_API_REFRESH_TOKEN_STORAGE_KEY=refresh_token


